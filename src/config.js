export const CONFIG = {
  globalStatsToDisplay: {
    'concurrent_vms': {
      'name': 'Concurrent VMs',
      'processing': (value) => ( value ),
    },
    'cumulative_svms': {
      'name': 'Cumulative SVM Hours',
      'processing': (value) => ( value ),
    }
  }
}
